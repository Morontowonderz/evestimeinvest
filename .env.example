# Public canonical origin. Do not include a trailing slash.
VITE_SITE_URL=https://YOUR_DOMAIN
VITE_WHATSAPP_NUMBER=

# Existing Redis integration — server-only
KV_URL=
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=
REDIS_URL=

# Add when backend features are implemented
DATABASE_URL=
# Server-only values. Set these in Vercel; never prefix secrets with VITE_.
AUTH_SECRET=
CRON_SECRET=
